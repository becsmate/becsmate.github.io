{
  "name": "my-flask-react-app",
  "version": "1.0.0",
  "description": "Flask backend with React TypeScript frontend",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "clean": "rm -rf client/build client/node_modules",
    "heroku-postbuild": "npm run build"
  },
  "cacheDirectories": [
    "client/node_modules"
  ],
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  }
}
