{
  "name": "becsmate-personal-site",
  "version": "1.0.0",
  "description": "Personal website - Flask backend with React TypeScript frontend",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "clean": "rm -rf client/build client/node_modules",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "dev": "./scripts/dev.sh",
    "dev:docker": "docker-compose -f docker-compose.dev.yml up",
    "dev:frontend": "./scripts/dev-frontend.sh",
    "dev:backend": "./scripts/dev-backend.sh",
    "dev:init": "python scripts/init_db.py",
    "local:install": "cd client && npm install",
    "local:setup": "pip install -r requirements.txt && cd client && npm install"
  },
  "cacheDirectories": [
    "client/node_modules"
  ],
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
